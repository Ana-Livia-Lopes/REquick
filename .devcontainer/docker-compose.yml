version: "3.8"

services:
  app:
    image: mcr.microsoft.com/devcontainers/php:8.2
    volumes:
      - ../:/workspace
    command: sleep infinity
    ports:
      - "8000:8000"

  db:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: app